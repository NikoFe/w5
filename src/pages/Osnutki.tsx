import React from "react";
import Scrollable from "../components/Scrollable";

const Osnutki = () => {
  return (
    <div>
      <Scrollable top="osnutki" pageType="osnutki"></Scrollable>
    </div>
  );
};

export default Osnutki;
